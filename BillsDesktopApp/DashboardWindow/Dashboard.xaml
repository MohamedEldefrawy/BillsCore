<Window x:Class="BillsDesktopApp.DashboardWindow.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="لوحة القيادة" MinWidth="350" MinHeight="350" Width="400" Height="450" 
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="12"
        FontFamily="{materialDesign:MaterialDesignFont}" 
        ScrollViewer.HorizontalScrollBarVisibility="Auto"
        WindowStartupLocation="CenterScreen" HorizontalContentAlignment="Stretch" 
        VerticalContentAlignment="Stretch"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <DockPanel  Grid.Row="0" Grid.Column="1" FlowDirection="RightToLeft" HorizontalAlignment="Center" VerticalAlignment="Stretch">
            <Label x:Name="lblWelcome" Content="asdasdasd" 
                   HorizontalAlignment="Center" VerticalAlignment="Stretch" 
                   HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center"
                   Height="Auto" Width="Auto" MinHeight="43.5" FlowDirection="RightToLeft"/>
            <Label Name="lblUserId" Visibility="Hidden"/>
        </DockPanel>

        <DockPanel Grid.Row="0" Grid.Column="0">
            <Button                 
                x:Name="btnEdit" Content="تعديل الصفحة الشخصية"
                VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                Height="Auto" Width="Auto" MinHeight="43.5" Click="btnEdit_Click"/>
        </DockPanel>


        <DockPanel Grid.Row="1" Grid.Column="0">
            <Button 
                x:Name="btnProducts" Content="منتجات" 
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                Width="Auto" Height="Auto" MinHeight="130.2" Click="btnProducts_Click" 
                />
        </DockPanel>

        <DockPanel Grid.Row="1" Grid.Column="1">
            <Button 
                x:Name="btnCustomers" Content="عملاء"
                VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                MinHeight="130.2" Width="Auto" Height="Auto" Click="btnCustomers_Click"/>
        </DockPanel>

        <DockPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Button 
                x:Name="btnBills" Content="فواتير" 
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                Height="Auto" Width="Auto" MinHeight="130.2" Click="btnBills_Click"/>
        </DockPanel>

        <DockPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">
            <Button 
                x:Name="btnCompany" Content="الشركة" 
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                Height="Auto" Width="Auto" MinHeight="86.8" Click="btnCompany_Click"/>
        </DockPanel>

        <DockPanel Grid.Row="4" Grid.ColumnSpan="2">
            <Button x:Name="btnLogout" Content="تسجيل خروج"
                    VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                    VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                Grid.Row="3" Grid.ColumnSpan="2" Width="Auto" Height="Auto" MinHeight="43.5" Click="btnLogout_Click"/>
        </DockPanel>
    </Grid>
</Window>
